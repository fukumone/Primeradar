<p>
  三つ子素数（みつごそすう、prime triplet）もしくは三つ組素数とは、(p, p + 2, p + 6) または (p, p + 4, p + 6) の形をした、素数の三つ組のこと
</p>

<div class="error">
<p>
  <%= flash.alert %>
</p>
</div>

<%= form_tag [ :prime_triplet, :prime_index ], method: :get do %>
<br>
  三つ子素数 検索
  <%= text_field_tag :prime_triplet_from, "",maxlength: 6 %> 〜
  <%= text_field_tag :prime_triplet_to, "", maxlength: 6 %>
  <%= submit_tag "検索" %>
<% end %>

<% if @search_prime_triplet.present? %>
<h4 align="center">
<%= params[:prime_triplet_from] %>〜<%= params[:prime_triplet_to] %>の中の素数たち
<p>三つ子素数の合計：<%= number_with_delimiter(@search_prime_triplet.count) %></p>
</h4>

<table width="30%">
<% @search_prime_triplet.each do |num| %>
  <tr class="<%= cycle("odd", "even") %>">
  <td class="prime">
    <% num.each do |prime_number| %>
    <%= prime_number %>
    <% end %>
  </td>
  </tr>
  <% end %>
</table>
<% end %>

